/* 

create a table to display the records
create inputs to allow entries of data, description, amount
create add button, add event listener to add button when clicked:
read enetered data out of inputs
add to list of expenses , update total
update the output table


parentheis execute functions
*/

function submit() {
  console.log("submitted");
}

document.querySelector("#addExpense").addEventListener("click", submit);
